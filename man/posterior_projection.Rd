% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior-projection.R
\name{posterior_projection}
\alias{posterior_projection}
\alias{posterior_projection.ShrinkageTrees}
\alias{posterior_projection.CausalShrinkageForest}
\alias{posterior_projection.default}
\title{Lower-dimensional posterior projection}
\usage{
posterior_projection(object, ...)

\method{posterior_projection}{ShrinkageTrees}(object, ...)

\method{posterior_projection}{CausalShrinkageForest}(object, target = c("treatment", "prognostic"), ...)

\method{posterior_projection}{default}(
  object,
  X,
  family = c("linear", "additive", "tree"),
  covariates = NULL,
  penalty = c("none", "ridge", "lasso", "elastic_net"),
  alpha = 0.5,
  lambda = NULL,
  df_spline = 4,
  max_leaves = 6,
  weights = NULL,
  level = 0.95,
  scale_label = "response",
  target = "f",
  ...
)
}
\arguments{
\item{object}{A fitted \code{ShrinkageTrees} or \code{CausalShrinkageForest} object,
or (default method) an \code{S} x \code{n} matrix of posterior draws on the
additive scale.}

\item{...}{Passed on to the default method.}

\item{target}{Which function to project: \code{"treatment"} for the CATE surface
\eqn{\tau(x)}, or \code{"prognostic"} for \eqn{\mu(x)}.}

\item{X}{(default method) The \code{n} x \code{p} design the supplied draws
correspond to; column names label the projection. Not a \code{newdata}
argument -- it must come from somewhere when there is no fitted object
to read it from.}

\item{family}{\code{"linear"} projects onto a linear model, \code{"additive"} onto a
natural-spline additive model (per-covariate effect curves with posterior
bands), \code{"tree"} onto a CART partition.}

\item{covariates}{Optional character or integer vector selecting the
covariates to project onto. Default \code{NULL} uses all of them. No selection
is performed: choosing this subset is the user's job.}

\item{penalty}{Regularisation for the linear projection: \code{"none"} for
ordinary least squares, or \code{"ridge"}, \code{"lasso"}, \code{"elastic_net"}. The
penalised options are the ones that stay defined when the covariate set is
larger than \code{n}, which is the regime this package targets.
\code{family = "linear"} only.}

\item{alpha}{Elastic-net mixing parameter, \code{penalty = "elastic_net"}.
\code{alpha = 1} is the lasso, \code{alpha = 0} the ridge.}

\item{lambda}{Penalty level for the penalised projections. \code{NULL} or
\code{"1se"} cross-validates on the posterior mean and takes \code{lambda.1se},
\code{"min"} takes \code{lambda.min}, and a number is used directly.}

\item{df_spline}{Degrees of freedom per natural spline,
\code{family = "additive"}.}

\item{max_leaves}{Maximum number of leaves, \code{family = "tree"}.}

\item{weights}{Optional non-negative weights over the observations,
defining the population the projection is taken over. Default uniform.}

\item{level}{Credible level for all reported intervals.}

\item{scale_label}{Provenance label carried into the printed output
(default method).}
}
\value{
An object of class \code{"PosteriorProjection"}, a list with
\code{coefficients} (\code{S} x \code{q} draws of the projection coefficients),
\code{coef_summary}, \code{rho2} (an \code{S}-vector of summary \eqn{R^2}),
\code{covariates}, \code{structure}, and provenance fields.
}
\description{
Projects every posterior draw of the fitted function onto a simpler model,
following Woody, Carvalho and Murray (2021), and returns the resulting
posterior distribution over projections.
}
\details{
The projection is taken over the design the model was fitted to; there is no
\code{newdata} argument. Nor is there any covariate selection: by default the
projection uses every covariate, and a subset can be supplied through
\code{covariates}. Choosing that subset is the user's job.

The penalised projections are solved \strong{separately for every draw}, at a
common \code{lambda}: for each draw,
\eqn{\gamma^{(s)} = \arg\min \|f^{(s)} - X\gamma\|^2 + \lambda\|\gamma\|_1}.
The lasso is a nonlinear operator, so the projection of the posterior mean is
not the mean of the projections; solving per draw is what gives a genuine
posterior over sparse projections, with coefficients that are exactly zero in
some draws and not in others. It costs one glmnet solve per draw.

\code{lambda} indexes the summary family, so it is chosen once for all draws, by
cross-validating on the posterior mean. Holding out observations asks how
well a sparse linear summary generalises across the covariate space, which is
a real question even though a posterior draw carries no observation noise.

With \code{penalty = "ridge"} nothing is set to zero, so the result is a
regularised projection rather than a lower-dimensional summary.
}
\examples{
n <- 80; p <- 30
X <- matrix(rnorm(n * p), n, p,
            dimnames = list(NULL, paste0("x", 1:p)))
y <- 2 * X[, 1] - X[, 2] + rnorm(n)

fit <- HorseTrees(y = y, X_train = X, outcome_type = "continuous",
                  number_of_trees = 5, N_post = 50, N_burn = 25,
                  store_posterior_sample = TRUE, verbose = FALSE)

# Linear projection onto two covariates
pp <- posterior_projection(fit, family = "linear",
                           covariates = c("x1", "x2"))
pp

# Additive projection with per-covariate effect curves
pp_add <- posterior_projection(fit, family = "additive",
                               covariates = c("x1", "x2"))

\donttest{
# Sparse projection; lambda cross-validated on the posterior mean
if (requireNamespace("glmnet", quietly = TRUE)) {
  pp_las <- posterior_projection(fit, family = "linear",
                                 penalty = "lasso")
}

# Subgroup summary
if (requireNamespace("rpart", quietly = TRUE)) {
  pp_tree <- posterior_projection(fit, family = "tree", max_leaves = 4)
}
}
}
\references{
Woody, S., Carvalho, C. M., & Murray, J. S. (2021). Model
Interpretation Through Lower-Dimensional Posterior Summarization.
\emph{Journal of Computational and Graphical Statistics}, 30(1), 144-161.
}
\seealso{
\code{\link[=print.PosteriorProjection]{print.PosteriorProjection()}}, \code{\link[=plot.PosteriorProjection]{plot.PosteriorProjection()}}
}
